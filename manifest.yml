---
applications:
  - name: my-cap-app
    path: .
    memory: 512M
    buildpacks:
      - nodejs_buildpack
    env:
      -SAP_JWT_TRUST_ACL: '[{"clientid":"*"}]'
      -ALLOW_MOCKED_AUTH_HEADER: true
    services:
      - destination-service
      - connectivity
